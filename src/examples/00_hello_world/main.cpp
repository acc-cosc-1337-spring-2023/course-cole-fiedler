#include<iostream> // Tells c++ to bring in code that allows
// us to display to screen and receive keyboard input 

//using namespace std; Beginner way to import std library. Not best practices

using std::cout; //tells c++ to find std namespace and use cout from that library

// std::cout specifies cout from std library

int main()
{
    cout<<"Hello World!"; 
    return 0;
}
